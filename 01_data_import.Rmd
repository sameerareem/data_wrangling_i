---
title: "Data Import"
output: github_document
---

This file is for doing data import.

```{r}
library(tidyverse)
```

Import our first dataset

```{r}
litters_df = 
  read_csv(file = "data/FAS_litters.csv")

litters_df = janitor::clean_names(litters_df)
```

Import our second dataset.

```{r}
pups_df = 
  read_csv(file = "data/FAS_pups.csv", skip = 3) ##skip=3 to skip first 3 rows in csv file w no data

pups_df = janitor::clean_names(pups_df)
```

## Look at the data

```{r}
litters_df
pups_df
```

Head or tail

```{r}
head(litters_df, 20)  ## to look at first 20 rows

tail(pups_df, 5)  ## to look at last 5 rows
```

Skimming

```{r}
skimr::skim(pups_df)
```




